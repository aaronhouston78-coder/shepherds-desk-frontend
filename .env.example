# Copy this to .env.local for local development overrides.
# For local dev the Vite proxy handles /api routing — no variable needed.

# For production deployments, set this to your backend URL (no trailing slash):
# VITE_API_BASE=https://your-backend.railway.app
